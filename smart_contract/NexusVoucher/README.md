# Nexus Voucher Smart Contract Setup

## Prerequisites
- nodejs
- npm

## Setup
install packages
`npm install`

compile contracts
`npx hardhat compile`

run node
`npx hardhat node`

deploy contracts
`npx hardhat run scripts/deploy.js --network localhost`